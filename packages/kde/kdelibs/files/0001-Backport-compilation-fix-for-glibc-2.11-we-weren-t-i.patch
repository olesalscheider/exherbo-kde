From efa3245fb489f1fac278281bb6a4741b3462adfd Mon Sep 17 00:00:00 2001
From: mpyne <mpyne@283d02a7-25f6-0310-bc7c-ecb5cbfe19da>
Date: Sat, 7 Nov 2009 20:17:19 +0000
Subject: [PATCH] Backport compilation fix for glibc 2.11 (we weren't including enough headers) to
 KDE 4.3.4.  Originally committed in r1045794.

BUG:213223


git-svn-id: svn://anonsvn.kde.org/home/kde/branches/KDE/4.3/kdelibs@1046199 283d02a7-25f6-0310-bc7c-ecb5cbfe19da
---
 kdecore/fakes.c |   56 +++++++++++++++++++++++++++++++++++++++++++-----------
 1 files changed, 44 insertions(+), 12 deletions(-)

diff --git a/kdecore/fakes.c b/kdecore/fakes.c
index edd7d33..383c864 100644
--- a/kdecore/fakes.c
+++ b/kdecore/fakes.c
@@ -31,7 +31,7 @@
 
 
 #define KDE_open open
-#define KDE_mkdir mkdir 
+#define KDE_mkdir mkdir
 
 
 #ifndef HAVE_SETENV
@@ -39,10 +39,15 @@
 #ifdef HAVE_ALLOCA_H
 #include <alloca.h>
 #endif
-
+#ifdef HAVE_STRING_H
 #include <string.h>
+#endif
+#ifdef HAVE_STDLIB_H
 #include <stdlib.h>
+#endif
+#ifdef HAVE_UNISTD_H
 #include <unistd.h>
+#endif
 
 KDECORE_EXPORT int setenv(const char *name, const char *value, int overwrite) {
     int i;
@@ -67,11 +72,18 @@ KDECORE_EXPORT int setenv(const char *name, const char *value, int overwrite) {
 #ifdef HAVE_ALLOCA_H
 #include <alloca.h>
 #endif
-
+#ifdef HAVE_STRING_H
 #include <string.h>
+#endif
+#ifdef HAVE_STDLIB_H
 #include <stdlib.h>
+#endif
+#ifdef HAVE_ERRNO_H
 #include <errno.h>
+#endif
+#ifdef HAVE_UNISTD_H
 #include <unistd.h>
+#endif
 
 #ifndef environ
 extern char ** environ;
@@ -154,24 +166,32 @@ void srandom(unsigned int seed)
 {
     srand48(seed);
 }
-#endif
+#endif /* !HAVE_RANDOM */
 
 #ifndef HAVE_SETEUID
 int seteuid(uid_t euid)
 {
     return setreuid(-1, euid); /* Well, if you have neither you are in trouble :) */
 }
-#endif
+#endif /* !HAVE_SETEUID */
 
 #ifndef HAVE_MKSTEMPS
+#ifdef HAVE_SYS_TYPES_H
 #include <sys/types.h>
+#endif
 #ifdef HAVE_SYS_STAT_H
 #include <sys/stat.h>
 #endif
 #include <fcntl.h>
+#ifdef HAVE_STRING_H
 #include <string.h>
+#endif
+#ifdef HAVE_STRINGS_H
 #include <strings.h>
+#endif
+#ifdef HAVE_STDLIB_H
 #include <stdlib.h>
+#endif
 
 /* this is based on code taken from the GNU libc, distributed under the LGPL license */
 
@@ -244,12 +264,14 @@ KDECORE_EXPORT int mkstemp (char* _template)
 {
   return mkstemps( _template, 0 );
 }
-#endif
+#endif /* !HAVE_MKSTEMP */
 
 #ifndef HAVE_MKDTEMP
 
 #ifndef HAVE_MKSTEMPS
+#ifdef HAVE_SYS_TYPES_H
 #include <sys/types.h>
+#endif
 #ifdef HAVE_SYS_STAT_H
 #include <sys/stat.h>
 #endif
@@ -306,13 +328,18 @@ KDECORE_EXPORT char* mkdtemp (char* _template)
       value += 7777;
 
       if (!KDE_mkdir(_template,0700))
-	return _template;	
+	return _template;
     }
     return 0;
 }
 #endif /* !HAVE_MKDTEMP */
 
 #ifndef HAVE_STRLCPY
+
+#ifdef HAVE_STRING_H
+#include <string.h>
+#endif
+
 KDECORE_EXPORT unsigned long strlcpy(char* d, const char* s, unsigned long bufsize)
 {
     unsigned long len, ret = strlen(s);
@@ -325,12 +352,17 @@ KDECORE_EXPORT unsigned long strlcpy(char* d, const char* s, unsigned long bufsi
         }
     } else
 	memcpy(d, s, ret + 1);
-	
+
     return ret;
 }
-#endif
+#endif /* !HAVE_STRLCPY */
 
 #ifndef HAVE_STRLCAT
+
+#ifdef HAVE_STRING_H
+#include <string.h>
+#endif
+
 KDECORE_EXPORT unsigned long strlcat(char* d, const char* s, unsigned long bufsize)
 {
     char *cp;
@@ -350,7 +382,7 @@ KDECORE_EXPORT unsigned long strlcat(char* d, const char* s, unsigned long bufsi
 
     return ret;
 }
-#endif
+#endif /* !HAVE_STRLCAT */
 
 #ifndef HAVE_STRCASESTR
 /*
@@ -450,7 +482,7 @@ foundneedle:
 ret0:
   return 0;
 }
-#endif
+#endif /* !HAVE_STRCASESTR */
 
 #ifndef HAVE_TRUNC
 
@@ -467,4 +499,4 @@ KDECORE_EXPORT double trunc (double x)
 {
        return x < 0 ? -floor(-x) : floor(x);
 }
-#endif
+#endif /* !HAVE_TRUNC */
-- 
1.6.5.2.351.g0943

