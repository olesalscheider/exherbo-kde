Upstream: posted for review https://git.reviewboard.kde.org/r/118438/
From d2cc7b378136e544246bf38344ed88885c2b41ed Mon Sep 17 00:00:00 2001
From: Heiko Becker <heirecka@exherbo.org>
Date: Sat, 31 May 2014 12:32:30 +0200
Subject: [PATCH 1/2] Add an option to build only the baloo libraries

---
 src/CMakeLists.txt      |  15 ++++--
 src/file/CMakeLists.txt | 125 +++++++++++++++++++++++++-----------------------
 2 files changed, 74 insertions(+), 66 deletions(-)

diff --git a/src/CMakeLists.txt b/src/CMakeLists.txt
index de04402..6021130 100644
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -1,8 +1,13 @@
+option(BALOO_LIBRARIES_ONLY "If true, compiles only the Baloo libraries, without binaries, kioslaves." OFF)
+
 add_subdirectory(core)
 add_subdirectory(xapian)
 add_subdirectory(file)
-add_subdirectory(pim)
-#add_subdirectory(kioslaves)
-add_subdirectory(tools)
-add_subdirectory(runner)
-#add_subdirectory(queryparser)
+
+if (NOT BALOO_LIBRARIES_ONLY)
+    add_subdirectory(pim)
+    #add_subdirectory(kioslaves)
+    add_subdirectory(tools)
+    add_subdirectory(runner)
+    #add_subdirectory(queryparser)
+endif ()
diff --git a/src/file/CMakeLists.txt b/src/file/CMakeLists.txt
index 4f9fb9d..318b289 100644
--- a/src/file/CMakeLists.txt
+++ b/src/file/CMakeLists.txt
@@ -1,74 +1,77 @@
 kde_enable_exceptions()
 
 add_subdirectory(lib)
-add_subdirectory(search)
 
-set(file_SRCS
-    # Filewatch
-    filewatch.cpp
-    activefilequeue.cpp
-    metadatamover.cpp
-    database.cpp
+if (NOT BALOO_LIBRARIES_ONLY
+    add_subdirectory(search)
 
-    # File Indexer
-    fileindexer.cpp
-    fileindexerconfig.cpp
-    indexscheduler.cpp
-    indexingqueue.cpp
-    basicindexingqueue.cpp
-    fileindexingqueue.cpp
-    commitqueue.cpp
-    basicindexingjob.cpp
-    fileindexingjob.cpp
-    eventmonitor.cpp
-    util.cpp
+    set(file_SRCS
+        # Filewatch
+        filewatch.cpp
+        activefilequeue.cpp
+        metadatamover.cpp
+        database.cpp
 
-    # Common
-    priority.cpp
-    regexpcache.cpp
-    fileexcludefilters.cpp
-    # removabledeviceindexnotification.cpp
-    # removablemediacache.cpp
+        # File Indexer
+        fileindexer.cpp
+        fileindexerconfig.cpp
+        indexscheduler.cpp
+        indexingqueue.cpp
+        basicindexingqueue.cpp
+        fileindexingqueue.cpp
+        commitqueue.cpp
+        basicindexingjob.cpp
+        fileindexingjob.cpp
+        eventmonitor.cpp
+        util.cpp
 
-    main.cpp
-)
+        # Common
+        priority.cpp
+        regexpcache.cpp
+        fileexcludefilters.cpp
+        # removabledeviceindexnotification.cpp
+        # removablemediacache.cpp
 
-set(BUILD_KINOTIFY False)
-if(CMAKE_SYSTEM_NAME MATCHES "Linux")
-    set(BUILD_KINOTIFY True)
-    set(file_SRCS
-        ${file_SRCS}
-        kinotify.cpp
+        main.cpp
     )
-    add_definitions(-DBUILD_KINOTIFY)
-endif()
 
-add_executable(baloo_file ${file_SRCS})
+    set(BUILD_KINOTIFY False)
+    if(CMAKE_SYSTEM_NAME MATCHES "Linux")
+        set(BUILD_KINOTIFY True)
+        set(file_SRCS
+            ${file_SRCS}
+            kinotify.cpp
+        )
+        add_definitions(-DBUILD_KINOTIFY)
+    endif()
 
-target_link_libraries(baloo_file
-    Qt5::Sql
-    Qt5::DBus
-    KF5::KIOWidgets
-    KF5::IdleTime
-    KF5::I18n
-    KF5::Solid
-    KF5::Auth
-    KF5::FileMetaData
-    KF5::Crash
-    KF5::ConfigCore
-    ${XAPIAN_LIBRARIES}
-    baloocore
-    baloofiles
-    balooxapian
-)
+    add_executable(baloo_file ${file_SRCS})
 
-install(TARGETS baloo_file ${INSTALL_TARGETS_DEFAULT_ARGS})
-install(FILES baloo_file.desktop DESTINATION ${AUTOSTART_INSTALL_DIR})
-install(FILES org.kde.baloo.file.indexer.xml DESTINATION ${DBUS_INTERFACES_INSTALL_DIR})
+    target_link_libraries(baloo_file
+        Qt5::Sql
+        Qt5::DBus
+        KF5::KIOWidgets
+        KF5::IdleTime
+        KF5::I18n
+        KF5::Solid
+        KF5::Auth
+        KF5::FileMetaData
+        KF5::Crash
+        KF5::ConfigCore
+        ${XAPIAN_LIBRARIES}
+        baloocore
+        baloofiles
+        balooxapian
+    )
+
+    install(TARGETS baloo_file ${INSTALL_TARGETS_DEFAULT_ARGS})
+    install(FILES baloo_file.desktop DESTINATION ${AUTOSTART_INSTALL_DIR})
+    install(FILES org.kde.baloo.file.indexer.xml DESTINATION ${DBUS_INTERFACES_INSTALL_DIR})
 
-add_subdirectory(autotest)
-add_subdirectory(tests)
-add_subdirectory(extractor)
-add_subdirectory(kcm)
-add_subdirectory(inotify)
-add_subdirectory(cleaner)
+    add_subdirectory(autotest)
+    add_subdirectory(tests)
+    add_subdirectory(extractor)
+    add_subdirectory(kcm)
+    add_subdirectory(inotify)
+    add_subdirectory(cleaner)
+endif()
-- 
2.0.0

